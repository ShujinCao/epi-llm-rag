[project]
name = "lit-rag-extractor"
version = "0.1.0"
description = "Question-driven prevalence/incidence extraction with E5 + FAISS + GPT"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
